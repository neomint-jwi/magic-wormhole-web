services:
  wormhole-web:
    image: ghcr.io/neomint-research/magic-wormhole-web:latest
    ports:
      - "8080:8080"
    environment:
      - PORT=8080
    restart: unless-stopped
    # Temp directory for file transfers
    tmpfs:
      - /tmp:size=500M
